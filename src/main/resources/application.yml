spring:
  profiles:
    default: dev
  jackson:
    time-zone: Asia/Seoul

jwt:
  secret: ${JWT_SECRET}

springdoc:
  override-with-generic-response: false

enrollment:
  max-courses: 10

queue:
  key: "enrollment:queue"
